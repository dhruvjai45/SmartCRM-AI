# Dev setup - SmartCRM-AI

Prereqs:
- Docker & Docker Compose installed
- Node.js 18+ and npm/yarn/pnpm (for later backend)
- Git

Quick start (fresh dev machine):

1. Copy env:
2. Start containers:
3. Wait for Postgres to be ready:
4. Run migration & seed (if not auto-run):
5. Check DB:
6. Mailhog UI: http://localhost:8025
MinIO Console: http://localhost:9001 (default creds in .env.example)

