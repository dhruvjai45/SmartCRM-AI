# Migrations

This folder contains SQL migrations that initialize the SmartCRM-AI schema.

Usage (local dev with Docker Compose):

1. Copy `.env.example` -> `.env` and adjust values if needed.
2. Start the dev stack: